{
  "name": "khatanepal",
  "version": "0.1.0",
  "private": true,
  "description": "KhataNepal — enterprise POS & retail management platform for Nepali supermarkets",
  "packageManager": "pnpm@11.8.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @khatanepal/api prisma:generate",
    "db:migrate": "pnpm --filter @khatanepal/api prisma:migrate",
    "db:seed": "pnpm --filter @khatanepal/api prisma:seed",
    "db:studio": "pnpm --filter @khatanepal/api prisma:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "prettier": "^3.4.2"
  }
}
