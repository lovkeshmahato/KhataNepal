{
  "name": "khatanepal",
  "version": "0.1.0",
  "private": true,
  "description": "KhataNepal — enterprise POS & retail management platform for Nepali supermarkets",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "chmod -R +x node_modules/.bin apps/*/node_modules/.bin packages/*/node_modules/.bin 2>/dev/null || true",
    "dev": "turbo run dev",
    "build": "pnpm --filter @khatanepal/types build && pnpm --filter @khatanepal/api build && pnpm --filter @khatanepal/web build",
    "build:turbo": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @khatanepal/api prisma:generate",
    "db:migrate": "pnpm --filter @khatanepal/api prisma:migrate",
    "db:seed": "pnpm --filter @khatanepal/api prisma:seed",
    "db:studio": "pnpm --filter @khatanepal/api prisma:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "prettier": "^3.4.2"
  }
}
